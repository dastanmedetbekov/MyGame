name: C/C++ CI (Deprecated - Use build-game.yml)

# This workflow is deprecated and has been replaced by build-game.yml
# Please use the new workflow for building the game
on:
  # Disabled - workflow no longer runs automatically
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: configure
      run: ./configure
    - name: make
      run: make
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
